---
description: Create a conventional commit based on work done
---

# Commit Standard

## Rules
- When writing a commit message, strictly follow Conventional Commits formatting: `type(scope): message`.
- Ensure you run `cargo check` and `cargo fmt` before concluding work.
