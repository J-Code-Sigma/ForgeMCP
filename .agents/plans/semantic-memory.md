# Plan: Semantic Memory RAG (Pending)
- [ ] Create `pgvector` migration.
- [ ] Implement `DbClient` with `sqlx` in `src/db.rs`.
- [ ] Configure `fastembed-rs` `EmbeddingEngine` in `src/embeddings.rs`.
- [ ] Expose `save_to_memory` and `search_memory` MCP tools.
